import { Component, Input } from '@angular/core';

@Component({
    selector: 'et-box-light',
    templateUrl: '../html/box-light.html',
    styles: ['../box.css']
})

export class BoxLightComponent{
    // @Input() text;
}